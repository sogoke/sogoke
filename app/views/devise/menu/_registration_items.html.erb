<% if user_signed_in? %>
  <li>
  <%= link_to('我的帐号', base_settings_path) %>
  </li>
  <li>
  <%= link_to("手信(#{current_user.received_messages.unread.count})", messages_path) %>
  </li>
<% else %>
  <li>
  <%= link_to('注册', new_invitation_path)  %>
  </li>
<% end %>