<h1>Listing preferences</h1>

<table>
  <tr>
    <th>Mail on receiving message</th>
    <th>Mail on being followed</th>
    <th>Message from everyone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @preferences.each do |preference| %>
  <tr>
    <td><%= preference.mail_on_receiving_message %></td>
    <td><%= preference.mail_on_being_followed %></td>
    <td><%= preference.message_from_everyone %></td>
    <td><%= link_to 'Show', preference %></td>
    <td><%= link_to 'Edit', edit_preference_path(preference) %></td>
    <td><%= link_to 'Destroy', preference, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Preference', new_preference_path %>
