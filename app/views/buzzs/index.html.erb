<h1>Listing buzzs</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @buzzs.each do |buzz| %>
  <tr>
    <td><%= buzz.content %></td>
    <td><%= buzz.user_id %></td>
    <td><%= link_to 'Show', buzz %></td>
    <td><%= link_to 'Edit', edit_buzz_path(buzz) %></td>
    <td><%= link_to 'Destroy', buzz, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Buzz', new_buzz_path %>
